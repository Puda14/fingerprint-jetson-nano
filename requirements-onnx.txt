# Optional ONNX fallback dependencies.
# Use this only in a dedicated ONNX/dev environment, not in the default
# Jetson TensorRT setup that reuses system NumPy/OpenCV packages.

onnxruntime>=1.8.0,<2.0
