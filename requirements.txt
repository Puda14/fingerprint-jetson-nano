# ===========================================================================
# MDGT Edge Fingerprint Verification System - Python Dependencies
# ===========================================================================

# --- Web framework & server ------------------------------------------------
fastapi==0.83.0
uvicorn[standard]==0.16.0

# --- Async / networking ----------------------------------------------------
asyncssh>=2.5.0,<3.0.0
websockets>=8.0,<10.0
aiofiles>=0.8.0,<1.0.0

# --- Data validation -------------------------------------------------------
pydantic<2.0.0
# pydantic-settings: not needed for pydantic v1
python-dateutil>=2.8.2,<3.0.0

# --- Cryptography ----------------------------------------------------------
cryptography>=3.4.0,<44.0

# --- Numerical / ML --------------------------------------------------------
numpy>=1.19.0,<2.1
opencv-contrib-python>=4.5.0,<5.0
pillow>=8.0.0,<10.0
faiss-cpu>=1.7.0,<2.0
onnxruntime>=1.8.0,<2.0

# --- CLI --------------------------------------------------------------------
click>=8.1.0,<9.0

# --- Configuration ----------------------------------------------------------
pyyaml>=6.0,<7.0

# --- MQTT (orchestrator connectivity) ---------------------------------------
paho-mqtt>=1.6.0,<3.0

# --- GUI (PyQt5) ------------------------------------------------------------
# PyQt5>=5.15.0,<6.0
requests>=2.28.0,<3.0
websocket-client>=1.6.0,<2.0  # for WebSocket streaming

# --- Utilities (optional but recommended) -----------------------------------
httpx>=0.18.0,<1.0           # async HTTP client for health-checks / sync
python-multipart>=0.0.5      # file uploads via FastAPI
