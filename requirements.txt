# ===========================================================================
# MDGT Edge Fingerprint Verification System - Python Dependencies
# ===========================================================================

# --- Web framework & server ------------------------------------------------
fastapi==0.83.0
uvicorn[standard]==0.16.0

# --- Async / networking ----------------------------------------------------
asyncssh>=2.5.0,<3.0.0
websockets>=8.0,<10.0
aiofiles>=0.8.0,<1.0.0

# --- Data validation -------------------------------------------------------
pydantic<2.0.0
# pydantic-settings: not needed for pydantic v1
python-dateutil>=2.8.2,<3.0.0

# --- Cryptography ----------------------------------------------------------
# cryptography: use the Jetson/Ubuntu system package in the default Python 3.6
# TensorRT environment. Do not install/upgrade it from pip here, otherwise the
# shared venv can pull a newer build that only emits deprecation noise or
# requires extra toolchain pieces on Nano.

# --- Numerical / ML --------------------------------------------------------
# numpy>=1.19.0,<2.1 # Bị comment out để dùng numpy 1.13.3 có sẵn của Jetpack (tránh lỗi Illegal instruction)
# opencv-contrib-python: use system OpenCV on Jetson Nano (pre-installed by NVIDIA with CUDA)
pillow>=8.0.0,<10.0
# faiss-cpu: PyPI does not have ARM64 wheels for Python 3.6. You must compile from source
# onnxruntime is intentionally excluded from the default Jetson TensorRT setup.
# On Jetson Nano / Python 3.6 we rely on the system NumPy/OpenCV stack, while
# pip onnxruntime often pulls in an incompatible binary combination and dirties
# the shared venv. Install it only in a separate ONNX/dev environment if needed.

# --- CLI --------------------------------------------------------------------
click>=7.0,<8.1.0

# --- Configuration ----------------------------------------------------------
pyyaml>=5.4,<7.0

# --- MQTT (orchestrator connectivity) ---------------------------------------
paho-mqtt>=1.5.0,<2.0

# --- GUI (PyQt5) ------------------------------------------------------------
# PyQt5>=5.15.0,<6.0
requests>=2.20.0,<2.28.0
websocket-client>=0.59.0,<2.0  # for WebSocket streaming
pyusb>=1.0.2,<2.0              # USB sensor access

# --- Utilities (optional but recommended) -----------------------------------
httpx>=0.18.0,<1.0           # async HTTP client for health-checks / sync
python-multipart>=0.0.5      # file uploads via FastAPI
